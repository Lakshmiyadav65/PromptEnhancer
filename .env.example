# Copy this file to `.env` (in this same directory) and put your real key there.
# `.env` is gitignored — your real key never gets committed.
# This file is the template — keep the placeholder value below.

# Groq API key — get one free at https://console.groq.com/keys
GROQ_API_KEY=gsk_your_key_here
